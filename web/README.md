# Shapley Value Explainability App

  This is web frontend for Shapley Value Explainability App.

## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
